# Copy this file to deploy/.env and fill in real values. deploy/.env is
# gitignored — the upstream credential must never enter the repository.
#
# See openspec/changes/add-measurement-api/design.md, "Configuration", for
# the full meaning of each variable, and docs/how-to/deploy-facade.md for
# the deploy runbook.

# --- Edge (Caddy) ---------------------------------------------------------

# Public hostname this facade is reachable at. Caddy requests a TLS
# certificate for exactly this name; DNS must already point here.
NETNL_PUBLIC_HOST=facade.example.org

# Name of the docker network the upstream batch instance's own compose
# stack created for itself (e.g. `<instance-project>_default`). The
# instance must not publish a public port on it — this facade is the only
# public path to it. Discover it with `docker network ls` on the instance
# host.
NETNL_UPSTREAM_NETWORK=<instance-internal-network-name>

# --- Upstream (private batch instance) ------------------------------------

# Base URL of the batch instance's v2 API, reachable over
# NETNL_UPSTREAM_NETWORK — not a public address.
NETNL_UPSTREAM_ENDPOINT=https://<instance-intern>/api/batch/v2

# Batch credential issued on the upstream instance (see
# docs/reference/self-hosted.md). Server-side only; never exposed to
# tenants of this facade.
NETNL_UPSTREAM_USERNAME=<vul in>
NETNL_UPSTREAM_PASSWORD=<vul in>

# Set to 1 only if the internal hop to the upstream instance is plain HTTP
# (e.g. an isolated docker network you trust). Leave unset for https.
# NETNL_ALLOW_HTTP=1

# --- Facade storage ---------------------------------------------------------

# SQLite path inside the container. compose.yaml already sets this to the
# mounted volume path (/data/netnl.sqlite3) — no need to repeat it here
# unless overriding compose.yaml.
# NETNL_DB=/data/netnl.sqlite3

# --- Tunables (defaults shown as comments; uncomment to override) ---------

# Provenance name sent as the X-Netnl-Instance header on every reply.
# NETNL_INSTANCE=netnl

# Submissions per credential per hour.
# NETNL_RATE_LIMIT=10

# Max domains accepted per request.
# NETNL_MAX_DOMAINS=500

# Max non-terminal (in-progress) runs per credential at once.
# NETNL_MAX_CONCURRENT=2

# Days a finished request stays retrievable before `prune` removes it.
# NETNL_RESULT_RETENTION_DAYS=7

# Days audit records are kept before `prune` removes them.
# NETNL_AUDIT_RETENTION_DAYS=90

# Seconds the metadata/report passthrough is cached.
# NETNL_METADATA_TTL=3600

# Upstream HTTP timeout, seconds.
# NETNL_TIMEOUT=30

# Per-domain length cap (DNS hostname length limit).
# NETNL_MAX_DOMAIN_LENGTH=253

# Total request body size cap, bytes.
# NETNL_MAX_BODY_BYTES=1048576

# Seconds a `reserving` row (upstream submit never completed) is allowed
# to pin a concurrency slot before `prune` clears it. Keep this short; the
# prune cron cadence bounds the real-world grace window — see
# docs/how-to/deploy-facade.md.
# NETNL_RESERVING_GRACE_SECONDS=300

# Opt-in: contact value published on GET /.well-known/security.txt
# (RFC 9116), e.g. a mailto: URI or an https: URL. Unset by default — the
# route does not exist at all until this is set (a caller gets the
# ordinary 501 not-implemented for the path instead).
# NETNL_SECURITY_CONTACT=mailto:security@example.org

# --- Demo route (opt-in) ---------------------------------------------------
# See openspec/changes/add-demo-run/design.md and docs/how-to/demo-run.md
# for the full runbook. Unset NETNL_DEMO_ENABLED means the /demo/* routes do
# not exist at all (the ordinary 501 not-implemented catch-all) — every
# other variable below is then ignored, not even read.

# Master switch. NETNL_DEMO_ALLOWED_ORIGIN and NETNL_DEMO_TENANT must also be
# set for the route to exist at all.
# NETNL_DEMO_ENABLED=1

# Bare https://host[:port] origin the demo page is actually served from — no
# path, no trailing slash. The facade echoes this back verbatim as
# Access-Control-Allow-Origin.
# NETNL_DEMO_ALLOWED_ORIGIN=https://demo.example.org

# Dedicated, never-issued-to-anyone credential username the demo runs under.
# If this were ever also a real tenant's own username, the demo's read
# routes would become an anonymous read path into that tenant's runs.
# NETNL_DEMO_TENANT=netnl-demo

# Accepted demo submissions per rolling hour, across all visitors.
# NETNL_DEMO_MAX_PER_HOUR=6

# Accepted demo submissions per client-IP bucket per rolling hour.
# NETNL_DEMO_PER_IP_PER_HOUR=2

# GET status/results polls per client-IP bucket per rolling hour — bounds
# polling separately from NETNL_DEMO_PER_IP_PER_HOUR's bound on submissions.
# NETNL_DEMO_POLLS_PER_IP_PER_HOUR=120

# Seconds the same domain is blocked from a second demo submission.
# NETNL_DEMO_DOMAIN_COOLDOWN_SECONDS=900

# Max non-terminal demo runs at once.
# NETNL_DEMO_MAX_CONCURRENT=2

# Hours a finished demo run stays retrievable before prune removes it.
# NETNL_DEMO_RETENTION_HOURS=24

# Header the edge sets the caller's real IP into, trusted as-is for the
# per-IP buckets above.
# NETNL_DEMO_CLIENT_IP_HEADER=CF-Connecting-IP

# --- Supporter-key webhook bridge (opt-in) --------------------------------
# See openspec/changes/add-supporter-issuance/design.md and
# docs/how-to/supporter-webhook.md for the full runbook. Unset
# NETNL_BMC_WEBHOOK_SECRET means POST /webhooks/bmc does not exist at all
# (the ordinary 501 not-implemented catch-all) — every other variable below
# is then ignored, not even read.

# Master switch. Shared secret configured on the Buy Me a Coffee dashboard;
# generate with `openssl rand -hex 32`. Never logged. Must be at least 32
# characters (a floor against an obviously-too-short value, not a strength
# target) — the facade refuses to start below that.
# NETNL_BMC_WEBHOOK_SECRET=<vul in>

# Header BMC signs the payload into. Check the exact name on your BMC
# dashboard's webhook configuration and override if it differs.
# NETNL_BMC_SIGNATURE_HEADER=X-Signature-Sha256

# Byte cap on the raw webhook body.
# NETNL_BMC_MAX_BODY_BYTES=65536

# Only for the integration test against a real BMC test delivery — leave
# unset in production so a test-mode delivery is ignored, not issued.
# NETNL_BMC_ACCEPT_TEST_MODE=1

# Minimum donation amount (decimal string) that triggers issuance; donations
# below this are acknowledged and ignored. 0 means every live donation mints
# a key. Default: 2.
# NETNL_SUPPORTER_MIN_AMOUNT=2

# Optional expected currency (case-insensitive match); unset accepts any.
# NETNL_SUPPORTER_CURRENCY=EUR

# Ceiling on new issuances started per rolling hour.
# NETNL_SUPPORTER_MAX_PER_HOUR=20

# A transaction failing delivery this many times is parked (503 forever)
# until an operator intervenes.
# NETNL_SUPPORTER_MAX_ATTEMPTS=3

# How long a not-yet-delivered issuance is treated as "still in flight"
# before a retried/duplicate delivery is allowed to take it over — default
# is derived from NETNL_SMTP_TIMEOUT (8x it, plus a 30s margin: a full
# send is several sequential round trips, each individually bounded by
# that timeout). Override only if your relay's own behaviour needs a
# different value.
# NETNL_SUPPORTER_LEASE_SECONDS=150

# Prefix for generated supporter usernames (<prefix><8 hex chars>).
# NETNL_SUPPORTER_USERNAME_PREFIX=supporter-

# Optional operator address for a short post-delivery notification mail
# (no password, no supporter PII). Unset: no notification is sent.
# NETNL_SUPPORTER_NOTIFY=operator@example.org

# The facade's own public batch endpoint URL, interpolated into the
# credential mail sent to the donor. Required once the bridge is enabled.
# NETNL_PUBLIC_ENDPOINT=https://facade.example.org

# SMTP used to deliver the credential mail. Host/From are required once the
# bridge is enabled; username/password are optional (some relays
# authenticate by network origin instead).
# NETNL_SMTP_HOST=<vul in>
# NETNL_SMTP_PORT=587
# NETNL_SMTP_USERNAME=<vul in>
# NETNL_SMTP_PASSWORD=<vul in>
# NETNL_SMTP_FROM=netnl@example.org
# NETNL_SMTP_MODE=starttls
# NETNL_SMTP_TIMEOUT=15
# Only if NETNL_SMTP_MODE=plaintext is genuinely intended (e.g. a
# localhost-only relay) — plaintext mode puts the relay password
# (NETNL_SMTP_PASSWORD, if set) on the wire unencrypted, same as the
# credential mail body itself:
# NETNL_SMTP_ALLOW_PLAINTEXT=1
